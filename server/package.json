{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "set ENVIRONMENT=\"DEVELOPMENT\" && set EXPRESS_PORT=8080 && nodemon src",
    "start": "set ENVIRONMENT=\"DEVELOPMENT\" && set EXPRESS_PORT=8080 && node src",
    "production": "set ENVIRONMENT=\"PRODUCTION\" && set EXPRESS_PORT=8080 && node src",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma:pull-dev": "dotenv -e .env.development prisma db pull",
    "prisma:pull-prod": "dotenv -e .env.production prisma db pull"
  },
  "type": "commonjs",
  "author": "rjsdn",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.2.0"
  }
}
